// Add all numbers from to to n inclusive
function addUpTo(n) {
	return (n * (n + 1)) / 2;

	// let total = 0;
	// for (let i = 1; i <= n; i++) {
	// 	total += i;
	// }
	// return total;
}

console.log(addUpTo(6));
